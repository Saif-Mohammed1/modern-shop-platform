import noBadEventHandlers from "./rules/no-bad-event-handlers";
import noNestedButton from "./rules/no-nested-buttons";
export default {
  rules: {
    "no-bad-event-handlers": noBadEventHandlers,
    "no-nested-buttons": noNestedButton,
  },
};
