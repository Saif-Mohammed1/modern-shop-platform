enum Lang {
  uk = 'uk',
  en = 'en',
}
export const lang: Lang = Lang.uk;

// import { cookies } from "next/headers";

// const lang: Lang = (cookies().get("lang")?.value as Lang) || Lang.en;

// export { lang };
